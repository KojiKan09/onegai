# 費用メモ — 設計メモ

同居する大学生(受け取る側)と母親(渡す側)のあいだの、生活費・必要経費のやりとりを一か所にまとめるアプリ。

- モック: `index.html` をブラウザで開く(ビルド不要)。右上で視点を切り替える。
  データは `localStorage['onegai:mock2']` に保存され、設定の「デモデータを初期化」で戻る。
- 監修: 心理学者役・一般ユーザー役(母親・大学生)のサブエージェントによるレビュー(2026-09-22)を統合。

## 目的(2026-09-22 に訂正)

当初は「母親が同じものを二度渡してしまうのを防ぐ」という前提で作ったが、**これは誤りだった**。

実際の問題は逆で、**本来なら母親が出してくれるはずの出費を、大学生が言い出せずに自分の財布から払ってしまい、
手元のお金が不必要に減っている**こと。依頼者の言葉で「お金がもったいない」。

したがって主目的は **頼み損ねをなくすこと**。母親の負担軽減でも二重払い防止でもない。
副次的な制約として「母親が学生にお金を渡すことに嫌な感情を持つデザインにしない」は引き続き有効。

> 「このアプリの敵は親が忘れることじゃなくて、自分が言い出せないことです」(大学生役)
> 「あなたが黙って払ったぶんを、私は知らないままでいたくない」(母親役)

## 中心機能: メモとまとめ送信

**記録する瞬間と、頼む瞬間を切り離す。** これが設計の核。

> 「すぐ飛ぶと思うと余計に押せない。今これ送ったら親のスマホ鳴るんだよな、が指を止めてる」(大学生役)

- メモの間は**相手に通知が飛ばない**。書くことの社会的コストをゼロにする。
- あとから複数を選び、**1回でまとめて送る**。頼む回数が減れば、支払う気まずさの回数も減る。
- 母親には**1通知に統合**して届く。都度鳴らさない。

心理学的根拠: 記録は低コスト行動で、言いそびれを在庫化し侵入思考を下げる(Zeigarnik効果 / 実行意図)。
依頼の承諾率は実際より最大50%低く見積もられる(Flynn & Lake)ため、頼む回数を集約すると過小評価の影響も薄まる。

### 入力の制限時間

大学生役が離脱条件を数値で示した。**5秒以内、10秒を超えたら使わない。**

- 入力欄は**金額が最初**。「品目は後から思い出せるけど金額は忘れる」。
- **品目は任意**。金額だけでメモできる。
- 補足は初期状態で画面に出さない。「補足を追加」で開く。
- 金額欄は画面を開いた時点でフォーカス済み。

## 設計ルール

### 合計金額を送信前に出さない

両当事者が独立に、強い言葉で一致した唯一の禁止事項。

> 「合計を見た瞬間、間違いなく数件チェックを外します。そこで外したやつが、まさに今まで自腹だったやつ。目的と逆行します」(大学生役)
> 「合計が上に大きく出ていたら、確実に請求書です」(母親役)

送信前の一覧にも確認シートにも合計を出さない。送信後と履歴でなら出してよい。

### 母親の判断は一括と個別の両方

- 既定は「全部そのまま渡す」。その下に1件ずつの判断。実運用は八割が一括。
- 「全部OKしかないと、1件のために全部を止めることになって、結局その場で返事できません」(母親役)
- 判断機能そのものは**必須**。削ると母親が使わない。
  > 「ここを削ったら私はただの財布です。記録だけのアプリなら私は入れません」(母親役)

### 自腹記録(大学生専用)

- メモを消す代わりに「自分で払った」へ分類。母親には**一切見せない**。
- 「月末に今月これだけ自腹だったと自分で見たい。それを見たら次は言える気がする」(大学生役)
- 累計額を大きく出さない。

### 一部を渡すときの残額

- 「残りは自分で出してもらう」「後日渡す」を母親が選ぶ。
- 後日渡すを選ぶと、残額分が**母親の約束として受け渡し待ちに入る**。学生が再度送る必要はない。
- 申し出額に取り消し線を引かない。差額を強調しない。

### 通知

| 宛先 | 内容 | 頻度 |
|---|---|---|
| 母親 | まとめ送信の到着 | 送信ごとに1通知(件数付き、合計額なし) |
| 母親 | メモに関する催促 | **一切送らない** |
| 大学生 | メモの件数 | 週1回・曜日固定・朝1回。0件なら無通知 |
| 双方 | 受け渡しの確認が片方だけ | 2日後に未チェック側へ1回のみ・朝9時 |

大学生への週次通知は**催促ではなく状態報告**。滞留日数・赤バッジ・「たまっています」は禁止。

## 文言

依頼者案「支給依頼」は3者とも不採用。「支給」は給付者と受給者の身分の非対称を語自体が宣言し、
大学生が自分を受給者としてラベルしてしまう(self-labeling)。母親側では「私が子どもに払うのは支給ではありません」。
そして決定的な指摘:

> 「こんな大げさな手続きを1980円で起こすのか、となって結局また自腹になる」(大学生役)

**重い語は小口を弾き、目的と正面から衝突する。**

「事務化すれば依頼の心理的コストが下がる」という仮説も棄却された。

> 「経費申請が楽なのは、言葉が硬いからじゃなくて相手が人じゃないからです。親相手だと何と書いても相手は親」(大学生役)

気まずさを下げるのは語ではなく構造。(a) 書いた瞬間に飛ばない (b) まとめて1回 (c) 返事を急かされない。

| 対象 | 語 |
|---|---|
| メモ(送信前・大学生のみ) | 「メモ」/ ボタン「メモしておく」 |
| 送信 | 「まとめて送る」「◯件を送る」 |
| 母親の画面 | **見出しに名詞を置かない**。「◯◯さんから 3件」 |
| 母親の判断 | 「この金額で渡す」「一部を渡す」「今回は見送る」「あとで」 |
| 受け渡し | 渡す側「渡した」/ 受け取る側「受け取った」 |
| 名詞が必要な箇所 | 「費用」 |

- **使わない語**: 「お願い」「支給」「依頼」「請求」「申請」「却下」「拒否」「未払い」「未処理」「期限」「催促」「下書き」「未送信」「一時保存」。
- 「下書き」を使わないのは「まだ未完成感が出て、書くのに構えます」(大学生役)ため。
- 「まだ」は**相手の行動を指す場合のみ禁止**。母親のホーム見出し「まだ見ていないもの」は主語が自分なので可。
- 「やめる」(旧「取り下げる」)。「見送る」は維持(「却下」だったら押せない、と母親役)。
- 役職名ではなく、設定で入れた呼び名を使う。

### 禁止

赤・×・警告アイコン / 未処理バッジ / 累計・グラフの常時表示 / 既読・経過日数 /
取り消し線や差額の強調 / 絵文字・幼児語 / 年齢を前提にした機能 / 承認をデフォルト選択にする /
見送り理由の入力欄 / 履歴で見送りだけを絞り込む機能。

## 状態モデル

| status | ラベル | 見える人 | 条件 |
|---|---|---|---|
| memo | メモ | 大学生のみ | 未送信 |
| self_paid | 自分で払った | 大学生のみ | 自腹で処理 |
| requested | 返事まち | 双方 | 送信済み・未返答 |
| handover | 受け渡し待ち | 双方 | 返答済み・両方未チェック |
| one_ticked | 確認待ち | 双方 | 片方のみチェック |
| done | 完了 | 双方 | 両方チェック |
| declined | 見送り | 双方 | 母親が見送り |
| cancelled | やめた | 大学生のみ | メモを取り消し |

status は保存せず、`sentAt / decision / selfPaid / cancelled / motherTickedAt / studentTickedAt` から導出する。

```
item: { id, title, amount, note, monthly,
        createdAt, sentAt, batchId,
        decision: 'full'|'partial'|'declined'|null, approvedAmount, remainder: 'self'|'later'|null,
        decidedAt, motherNote, motherTickedAt, studentTickedAt, studentNote,
        selfPaid, selfPaidAt, cancelled, reminderDismissed }
```

## 画面

- 共通: 下タブ「ホーム」「履歴」「設定」。ルーターなし(`state.screen` の判別共用体)。
- 大学生ホーム: 受け渡しリマインド → 毎月 → メモ(選択可) → 送ったもの → 最近の完了。
  下部は選択中なら「◯件を送る」、そうでなければ「メモしておく」。
- メモ画面: 金額(フォーカス済み) → 品目(任意・候補チップ) → 補足(折りたたみ)。
- 母親ホーム: 受け渡しリマインド → 毎月 → まだ見ていないもの(送信の束ごと) → 受け渡し待ち → 最近の完了。
- 履歴: 月送り。チップは大学生が「すべて/完了/自分で払った」、母親は「すべて/完了」。
- 設定: 呼び名 / テーマ / 履歴に月の合計を表示 / 毎月の生活費 / お知らせの説明 / デモデータを初期化。

## ビジュアルトークン

| role | Light | Dark |
|---|---|---|
| bg | #F7F5F1 | #161513 |
| surface | #FFFFFF | #1F1D1A |
| surface-raised | #FBFAF8 | #2A2723 |
| text | #1F1D1A | #EDE9E2 |
| muted | #6B665E | #A39D93 |
| border | #E4DFD7 | #3A362F |
| accent | #3D5A4C | #8FB3A0 |
| accent-soft | #E6EDE8 | #263B31 |
| positive / 地 | #2F6B4F / #E3EFE7 | #7FBF9A / #23352B |
| attention / 地 | #7A5314 / #F5EAD6 | #D9A85A / #3A2E1A |
| rest / 地 | #6B665E / #EEEBE6 | #A39D93 / #2E2B27 |

- 危険色(赤)はトークン自体を定義しない。
- タイポ: 0.75 / 0.875 / 1 / 1.125 / 1.375 / 1.75 rem、line-height 1.6、weight 400/500 のみ。金額は tabular-nums。
- アイコン: chevron / plus / check の3種のみ。モーション 150ms、シート 200ms、`prefers-reduced-motion` で無効。
- 寸法: 角丸 8、タップ 44、画面左右 24、max-width 560。

## 次フェーズ(実データ化)の要点

- Cloudflare Workers + D1。アプリ本体も同一オリジンで配信。定時処理は Cron Trigger。
- 紐づけは6文字コードとQR。単回・30分で失効・入力回数制限。アカウント登録もパスワードもなし。
- **iPhone はホーム画面に追加しないと通知が届かない。** 両レビュアーとも自力ではやらないと明言した。
  追加は学生が母親の端末でその場で代行し、**テスト通知が届くまでペアリングを完了扱いにしない**。
  母親側の購読が切れている間は学生側に「通知が届かない状態です」を常時表示する。
- 再接続は母親自身ではなく**学生が直す前提**で設計する。
  > 「半年後に機種変したとき全部消えて、私はそれを直せません」(母親役)

## 持ち越し

- 振込を併用するか。併用なら「振込済」が第三の受け渡し状態になる。
- レシート写真1枚でメモを成立させる(大学生役の要望)。
- 友人一家の金額データを依頼者のアカウントで預かることの扱い。アプリ内に一文を置き、削除の導線を用意する。

## 参考

- Flynn & Lake (2008) If You Need Help, Just Ask — 依頼の承諾率の過小評価
- Heyman & Ariely (2004) Effort for Payment: A Tale of Two Markets — 関係規範と市場規範
- Gollwitzer (1999) Implementation Intentions — 実行意図
