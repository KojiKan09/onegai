# 「お願い」設計メモ(Phase 1: 見た目のモック)

同居する大学生(受け取る側)と母親(支払う側)の、現金受け渡しを一か所にまとめる台帳。
目的は「同じものを二度渡す」「渡したか分からない」といった余分な支払いを減らすこと。

- モック: `index.html` をブラウザで開く(ビルド不要)。右上の「学生 / 母」で視点を切り替える。データは `localStorage['onegai:mock']` に保存され、設定の「デモデータを初期化」で戻る。
- 前提: 大人同士の対等な家計共有として設計する。母の年齢やスマホ習熟度は仮定しない。
- 監修: 心理学者役・UI/UXデザイナー役のサブエージェントによるレビュー(2026-09-20、大学生前提でやり直したもの)を以下に統合。

## 設計原則(監修の結論)

核: **関係規範を市場規範に切り替えない**(Heyman & Ariely 2004)。家庭内の金銭授受は善意で動いている。
台帳が金額・件数・経過日数を強調すると「取引」に格下げされ、戻りにくい。
台帳は母を監査するものではなく、**母が二度払いしないために母を守るもの**として枠付ける。

### 言葉づかい

| 場面 | 文言 |
|---|---|
| 学生の送信ボタン | 「お願いを送る」 |
| 母の判断(3択+保留) | 「この金額で渡す」「一部を渡す」(金額入力展開)「今回は見送る」+ テキスト「あとで」 |
| 母のひとこと欄 | 「ひとこと(任意)」 |
| 受け渡しチェック | 母「渡した」/ 学生「受け取った」 |
| 学生への決定表示 | 「渡す予定です」「¥8,000 を渡す予定です」「今回は見送りです」 |
| 母へのリマインド(2日後) | 「教科書 ¥4,200:渡し済みなら「渡した」をどうぞ」 |
| 学生へのリマインド(2日後) | 「教科書 ¥4,200:受け取っていれば「受け取った」をどうぞ」 |
| 見送りの確認 | 「今回は見送りますか?」「学生側には「見送り」と表示されます。」→「見送る」/「戻る」 |
| オンボーディング一文(母) | 「同じものを二度渡さないための、ふたりの受け渡しメモです」 |
| オンボーディング一文(学生) | 「頼んだこと・受け取ったことを一か所にまとめて、お互いの手間を減らします」 |

### ルール

- 「却下」ではなく「見送る」。却下は評価語で母を判定者・敵役にする。見送るは状況語で、後日の再依頼を妨げない。
- 「一部を渡す」は値切りではなく分担のフレーム。依頼額に取り消し線を引かない。差額を強調しない。
- 「あとで」は既読プレッシャーの逃げ道。押した項目はリマインド対象外。
- リマインドは**未チェック側にのみ、2日後に1回だけ**。主語を相手の名前にしない。「まだ」「未」「期限」「忘れ」を使わない。
- メモ欄は任意。理由の提示要求は信頼欠如のシグナル(自己決定理論)。
- 母ホームに出さないもの: 合計・件数・経過日数・既読・過去分。履歴と月合計は設定でONにした時だけ。
- 完了項目はホームから3日で自然に消える(履歴には残る)。達成感より静けさを優先。
- 月額の生活費: 同じ台帳に載せるが**返答ステップを飛ばす**。設定で金額と起票日を決め、毎月「受け渡し待ち」として起票。ホームでは「毎月」小セクションに分ける(臨時分が「上乗せ請求」に見えるのを防ぐ)。
- 受領後の自動「ありがとう」通知は入れない。学生の「受け取った」後に任意の一言欄(空でも可)。母には通知せず、次に開いた時に項目の下に表示。

### 禁止

赤・×・警告アイコン / 未処理バッジ / 累計・グラフの常時表示 / 既読・経過日数 / 取り消し線や差額の強調 /
絵文字・幼児語・「いいよ」系 / 文字サイズ専用設定など年齢前提の機能 / 承認をデフォルト選択にする / 見送り理由の入力欄。

## 状態モデル

| status | ラベル | 色役割 | 条件 |
|---|---|---|---|
| requested | 返答待ち | muted | 未返答 |
| handover | 受け渡し待ち | attention | 承認/一部の後、両方未チェック |
| one_ticked | 確認待ち | attention | 片方のみチェック |
| done | 完了 | positive | 両方チェック |
| declined | 見送り | rest | 母が見送り |
| withdrawn | 取り下げ | rest | 学生が取り下げ |

status は保存せず、`decision / withdrawn / motherTickedAt / studentTickedAt` から導出する。
一部承認は `approvedAmount` の有無で表現し、「¥8,000 / お願い ¥12,000」と並記する。

```
item: { id, title, amount, approvedAmount?, note, motherNote, studentNote, monthly,
        createdAt, decision: 'full'|'partial'|'declined'|null, decidedAt,
        motherTickedAt, studentTickedAt, withdrawn, reminderDismissed: { mother?, student? } }
```

## 画面

- 共通: 下タブ「ホーム」「履歴」「設定」。ルーターなし(`state.screen` の判別共用体)。
- 学生ホーム: リマインド(条件時)→ 毎月 → 進行中 → 最近の完了(3日以内・3件)→ 固定「お願いを追加」。
- 学生 お願いを追加: 品目(候補チップ 食費・日用品・教科書・定期券・学費関連・その他)→ 金額 → メモ(任意)。
- 学生 お願い詳細: 経過・母のひとこと・受け渡しチェック・受け取り後のひとこと・返答待ちのみ「取り下げる」。
- 母ホーム: リマインド(条件時)→ 毎月(カード内に「渡した」)→ 返答待ち → 受け渡し待ち(カード内に「渡した」)→ 最近の完了。
- 母 お願い詳細: 判断パネル(ラジオ3択・デフォルト未選択・ひとこと・「返答する」「あとで」)。見送りのみ確認シート。
- 履歴: 月送り・チップ「すべて/完了/見送り」・日付ごと。設定ONの時だけ「この月の受け渡し合計」。
- 設定: テーマ / 履歴に月の合計を表示 / 毎月の生活費(ON・金額・起票日)/ お知らせの固定文 / デモデータを初期化。

## ビジュアルトークン

| role | Light | Dark |
|---|---|---|
| bg | #F7F5F1 | #161513 |
| surface | #FFFFFF | #1F1D1A |
| surface-raised | #FBFAF8 | #2A2723 |
| text | #1F1D1A | #EDE9E2 |
| muted | #6B665E | #A39D93 |
| border | #E4DFD7 | #3A362F |
| accent | #3D5A4C | #8FB3A0 |
| accent-soft | #E6EDE8 | #263B31 |
| positive / 地 | #2F6B4F / #E3EFE7 | #7FBF9A / #23352B |
| attention / 地 | #7A5314 / #F5EAD6 | #D9A85A / #3A2E1A |
| rest / 地 | #6B665E / #EEEBE6 | #A39D93 / #2E2B27 |

- 危険色(赤)はトークン自体を定義しない。
- attention と rest の文字色は、地色に対して 4.5:1 以上になるよう監修案より暗く調整した。
- タイポ: 0.75 / 0.875 / 1 / 1.125 / 1.375 / 1.75 rem、line-height 1.6、weight 400/500 のみ。金額は tabular-nums。
- アイコン: chevron / plus / check の3種のみ(inline SVG、stroke 1.75)。
- モーション: 150ms、シート 200ms、`prefers-reduced-motion` で無効。
- 寸法: 角丸 8、タップ 44、画面左右 24、max-width 560、主要ボタン高さ 52。

## 次フェーズに持ち越す確認事項

1. 振込を併用するか。併用なら「振込済」が第三の受け渡し状態になる。
2. 「一部を渡す」の残りは「自分で」なのか「後日渡す」なのか。後者なら分割受け渡しが必要。
3. 二重払いは日用品(頻回小口)で起きているのか、月額と臨時の境界の曖昧さで起きているのか。後者なら「毎月分に含むもの」の明示が本丸。

## 参考

- Heyman & Ariely (2004) Effort for Payment: A Tale of Two Markets
- Prelec & Loewenstein (1998) The Red and the Black: Mental Accounting of Savings and Debt
- Joussemet, Landry & Koestner (2008) A Self-Determination Theory Perspective on Parenting
